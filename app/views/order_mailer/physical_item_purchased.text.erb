Hey Brian/Jason \ \ Just wanted to let you know you sold a physical item and need to be thinking about boxing up that junk and shipping it out! \ \ Order ID:
<%= @order.id %>
Order Confirmation ID:
<%= @order.request_id %>
Transaction ID:
<%= @order.transaction_id %>
User email:
<%= @user.email %>
\ Physical Items:
<% @order.line_items.each do |line_item| %>
  <% if line_item.product.physical_product? %>
    <%= "#{line_item.product.product_code} #{line_item.product.name}" %>
  <% end %>
<% end %>
\ Fulfill this order and mark it as fulfilled in the order fulfillment page dealy or else you'll keep getting reminder emails until you've obeyed. (Not implemented yet) \ \ Send to: \
<%= @order.address_name %>
\
<%= "#{@order.first_name} #{@order.last_name}" %>
\
<%= @order.address_street1 %>
<% if @order.address_street2? %>
  \
  <%= @order.address_street2 %>
<% end %>
\
<%= "#{@order.address_city}, #{@order.address_state} #{@order.address_zip}" %>
\
<%= @order.address_country %>